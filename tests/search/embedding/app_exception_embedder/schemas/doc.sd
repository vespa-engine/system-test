# Copyright Vespa.ai. All rights reserved.

schema doc {

  document doc {

    field text type string {
      indexing: index | summary
    }

  }

  field embedding type tensor<float>(x[5]) {
      indexing: input text | embed exception-embedder | attribute
  }

  rank-profile default {
    inputs {
      query(embedding) tensor<float>(x[5])
    }

    first-phase {
      expression: nativeRank
    }

    summary-features {
      query(embedding)
      attribute(embedding)
    }

  }

}
